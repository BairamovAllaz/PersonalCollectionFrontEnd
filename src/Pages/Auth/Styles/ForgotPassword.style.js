import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  MainGrid: {
    marginTop: "20px",
  },
  PasswordInput: {
    marginTop: "20px",
  },
  ChanegPassword : { 
    marginTop : "30px"
  }
});
