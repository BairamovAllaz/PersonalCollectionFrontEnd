import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  DivUserProfile: {
    display: "flex",
    alignItems: "center",
    cursor: "pointer",
  },
  Avatar: {
    width: "70px",
    height: "70px",
  },
  DivContaier: {
    display: "flex",
    alignItems: "center",
  },
});
