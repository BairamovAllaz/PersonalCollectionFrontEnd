import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  MainBox: {
    width: "100%",
    maxheight: "500px",
    display: "flex",
    flexWrap: "wrap",
    overflowY: "scroll",
  },
});
