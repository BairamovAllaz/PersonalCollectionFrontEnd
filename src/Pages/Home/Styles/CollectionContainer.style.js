import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  MainDiv: {
    width: "100%",
  },
  Title: {
    margin: "20px",
  },
  Carousel : { 
    width : "100%"
  }
});
